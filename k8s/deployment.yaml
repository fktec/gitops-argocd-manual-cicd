apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-deployment
  labels:
    app: api-component
spec:
  replicas: 1
  selector: 
    matchLabels:
      app: api-component
  template:
    metadata:
      labels:
        app: api-component
    spec:
      containers:
        - name: api-component
          image: nginx:1.27
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          resources:
            requests:
              memory: "64Mi"
              cpu: "100m"
            limits:
              memory: "128Mi"
              cpu: "200m"