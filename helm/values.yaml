deployment:
  appName: api-github-public-component
  image: nginx:1.27
  replicas: 1
  ports:
    - name: http
      containerPort: 80
      protocol: TCP
  resources:
    requests:
      memory: "64Mi"
      cpu: "100m"
    limits:
      memory: "128Mi"
      cpu: "200m"